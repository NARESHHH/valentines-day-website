import { defaults } from "./config.js";

export const appState = {
    yourName: defaults.yourName,
    lovedOneName: defaults.lovedOneName,
    quoteText: "",
    persistedImageUrl: "",
    uploadedImageUrl: null,
    uploadedImageDataUrl: "",
};
